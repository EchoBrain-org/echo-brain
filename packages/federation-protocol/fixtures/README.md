# Federation protocol fixtures

This directory will hold deterministic canonical-byte, digest, signature, and
verification fixtures shared by every protocol consumer. No pilot fixture is
promoted until it is independent of `src/experimental/n2`.
