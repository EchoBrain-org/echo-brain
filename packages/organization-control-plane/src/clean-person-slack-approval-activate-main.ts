/**
 * Compatibility entrypoint for the original operator command.
 *
 * New code should index the reaction-specific entrypoint. This facade remains
 * only so existing installations keep the same binary and dist path.
 */
import "./clean-person-slack-reaction-approval-activate-main.js";
