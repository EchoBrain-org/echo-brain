# Application

Use cases and their ports live here: Person identity sessions, employee
lifecycle, documents and document associations, the Person document source,
and project-context commands. They depend on ports and kernel domain rules,
never on a concrete adapter. Paginated reads use opaque, typed, bounded
cursors.
