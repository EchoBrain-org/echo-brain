# Presentation

Future transport and presentation implementations belong here. A central join
surface may provide landing/context only; the local installation owns signing,
private-key use, authority-result verification, and evidence storage.
Presentation calls application use cases and never queries persistence, signs
documents, or treats hidden controls as authorization. No protocol, framework,
or rendering model is selected yet.
