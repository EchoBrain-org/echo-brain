# Organization authority tests

Future suites will separate domain, application, persistence, transport,
presentation, artifact-isolation, and live system coverage as those boundaries
gain behavior. The first live gate covers the N=2/org=1 onboarding/access
slice; a later ingest-and-receipt gate is required for full parity with the
experimental pilot. The scaffold adds no skipped qualification tests.
