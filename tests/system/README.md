# One-organization system scenario

The first end-to-end scenario will bind an owner and employee through OIDC
Person sessions, verify each Person's scoped reads, revoke one membership, and
prove the other remains active.

Real meeting ingest and shared reasoning remain later scenarios. No executable
placeholder is kept until one of those scenarios is implemented.
