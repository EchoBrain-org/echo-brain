name: ci

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  # A newer commit may supersede a pull-request run. Every non-PR run has its
  # own group: GitHub concurrency retains at most one pending run per group,
  # even when cancel-in-progress is false.
  group: ci-${{ github.workflow }}-${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || format('run-{0}', github.run_id) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

env:
  PRODUCT_NODE_VERSION: "22.22.1"

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # Documentation proof links are verified against their exact
          # historical commits, not the current worktree alone.
          fetch-depth: 0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.PRODUCT_NODE_VERSION }}
          cache: npm
      - run: npm ci
      - run: npm run check

  person-client-package:
    name: macOS arm64 Person-client package
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.PRODUCT_NODE_VERSION }}
          cache: npm
      - run: npm ci
      - name: Pack and exercise the standalone Person client
        shell: bash
        run: |
          set -euo pipefail
          pack_dir="$RUNNER_TEMP/echo-person-client-package"
          mkdir -p "$pack_dir"
          chmod 700 "$pack_dir"
          npm run pack:person-client -- "$pack_dir"
          archive_count="$(find "$pack_dir" -maxdepth 1 -type f -name '*.tgz' | wc -l | tr -d ' ')"
          test "$archive_count" = 1
          archive="$(find "$pack_dir" -maxdepth 1 -type f -name '*.tgz' -print -quit)"
          package_version="$(node -p 'require("./src/product/person-client/package.json").version')"
          test "$(node -p 'process.platform')" = darwin
          test "$(node -p 'process.arch')" = arm64

          archive_list="$RUNNER_TEMP/echo-person-client-archive-files.txt"
          tar -tzf "$archive" > "$archive_list"
          ! grep -Eq '(^|/)services/|(^|/)dist/product/(cli|launchd-service|operator-lifecycle|processing)' "$archive_list"

          prefix="$RUNNER_TEMP/echo-person-client-install"
          person_home="$RUNNER_TEMP/echo-person-client-home"
          stdout_file="$RUNNER_TEMP/echo-person-client-stdout.json"
          stderr_file="$RUNNER_TEMP/echo-person-client-stderr.json"
          mkdir -p "$prefix" "$person_home"
          chmod 700 "$person_home"
          person_home="$(cd "$person_home" && pwd -P)"
          npm install --ignore-scripts --offline --no-audit --no-fund \
            --prefix "$prefix" "$archive"
          cli="$prefix/node_modules/.bin/echo-brain"

          test "$("$cli" --version)" = "$package_version"
          test "$("$cli" --help)" = 'usage: echo-brain person <command> [options]'
          set +e
          HOME="$person_home" "$cli" person records \
            > "$stdout_file" 2> "$stderr_file"
          status="$?"
          set -e
          test "$status" = 1
          test ! -s "$stdout_file"
          test -z "$(find "$person_home" -mindepth 1 -print -quit)"
          node -e '
            const fs = require("node:fs");
            const value = JSON.parse(fs.readFileSync(process.argv[1], "utf8"));
            if (
              value.ok !== false ||
              value.action !== "records" ||
              value.error !== "Person session is unavailable; sign in again"
            ) throw new Error(`unexpected result: ${JSON.stringify(value)}`);
          ' "$stderr_file"

          artifact_sha="$(shasum -a 256 "$archive" | awk '{print $1}')"
          runtime_profile="$RUNNER_TEMP/echo-clean-v1-runtime-profile.json"
          npm run profile:clean-v1-runtime -- \
            "$GITHUB_WORKSPACE/deploy/organization-authority" "$runtime_profile"
          node tools/clean-v1-runtime-profile.mjs validate "$runtime_profile" >/dev/null
          runtime_profile_sha="$(node tools/clean-v1-runtime-profile.mjs digest "$runtime_profile")"
          release_draft="$RUNNER_TEMP/echo-clean-v1-release-draft.json"
          release_record="$RUNNER_TEMP/echo-clean-v1-release.json"
          node -e '
            const fs = require("node:fs");
            fs.writeFileSync(process.argv[1], JSON.stringify({
              schema_version: 1,
              kind: "echo-clean-v1-release",
              release_id: `clean-v1-ci-${process.env.GITHUB_SHA.slice(0, 12)}`,
              released_at: "2026-08-24T00:00:00Z",
              baseline_compatibility_class: "clean-v1",
              source_sha: process.env.GITHUB_SHA,
              authority_image: {
                reference: `123456789012.dkr.ecr.us-west-2.amazonaws.com/echo-brain/authority@sha256:${"b".repeat(64)}`,
              },
              person_client: {
                package: "@echo-brain/person-client",
                version: process.argv[2],
                artifact_url: "https://downloads.example.test/echo-brain-person-client.tgz",
                artifact_sha256: process.argv[3],
              },
              runtime_profile: {
                artifact_url: "https://downloads.example.test/echo-brain-authority-runtime-profile.json",
                artifact_sha256: process.argv[4],
                profile_version: "clean-v1-profile-1",
              },
            }));
          ' "$release_draft" "$package_version" "$artifact_sha" "$runtime_profile_sha"
          node tools/clean-v1-release.mjs create "$release_draft" "$release_record"
          node tools/clean-v1-release.mjs validate "$release_record" >/dev/null
          kit="$pack_dir/echo-person-onboarding-kit.tar.gz"
          npm run kit:person-onboarding -- \
            --release "$release_record" \
            --artifact "$archive" \
            --runtime-node "$(node -p 'process.execPath')" \
            --output "$kit"
          kit_extract="$RUNNER_TEMP/echo-person-onboarding-kit"
          mkdir -p "$kit_extract"
          tar -xzf "$kit" -C "$kit_extract"
          kit_root="$(find "$kit_extract" -mindepth 1 -maxdepth 1 -type d -print -quit)"
          test -x "$kit_root/Start ECHO.command"
          test -x "$kit_root/node"
          "$kit_root/node" "$kit_root/verify-person-onboarding-kit.mjs" "$kit_root"

  authority-container:
    name: Organization authority container
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.PRODUCT_NODE_VERSION }}
          cache: npm
      - run: npm ci
      - name: Assert a native arm64 runner
        shell: bash
        run: test "$(uname -m)" = aarch64
      - name: Assert a clean checkout before the authority image build
        shell: bash
        run: test -z "$(git status --porcelain=v1 --untracked-files=all)"
      - name: Build the clean V1 authority image
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 # v6.18.0
        with:
          context: .
          file: deploy/organization-authority/Dockerfile
          platforms: linux/arm64
          load: true
          tags: echo-organization-authority:ci
          build-args: ECHO_SOURCE_SHA=${{ github.sha }}
          # This is an optimization only. Cache export failures must never
          # turn an otherwise valid proof red.
          # Pull-request scopes neither read nor write the canonical cache.
          # That prevents untrusted pull-request cache entries being consumed
          # by a main or manually dispatched proof.
          cache-from: type=gha,scope=authority-container-arm64-${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || format('ref-{0}', github.ref_name) }}
          cache-to: type=gha,mode=max,scope=authority-container-arm64-${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || format('ref-{0}', github.ref_name) }},ignore-error=true
      - name: Assert the authority image build left the checkout clean
        shell: bash
        run: test -z "$(git status --porcelain=v1 --untracked-files=all)"
      - name: Exercise the clean V1 authority image
        shell: bash
        run: |
          set -euo pipefail
          deployment="$GITHUB_WORKSPACE/deploy/organization-authority"
          compose="$deployment/compose.clean-v1.yaml"
          authority_local_state="$RUNNER_TEMP/echo-authority-ci-local"
          authority_local_args=()
          export ECHO_CLEAN_AUTHORITY_IMAGE=echo-organization-authority:ci
          export ECHO_CLEAN_AUTHORITY_UID="$(id -u)"
          export ECHO_CLEAN_AUTHORITY_GID="$(id -g)"
          export ECHO_CLEAN_AUTHORITY_HOST=localhost
          export ECHO_CLEAN_AWS_REGION=us-west-2
          export ECHO_CLEAN_AUTHORITY_LOG_GROUP=/echo-brain/authority/localhost
          cleanup() {
            if [[ "${#authority_local_args[@]}" -ne 0 ]]; then
              npm run authority:local -- down "${authority_local_args[@]}" \
                >/dev/null 2>&1 || true
            fi
          }
          trap cleanup EXIT

          authority_platform="$(docker image inspect --format '{{.Os}}' "$ECHO_CLEAN_AUTHORITY_IMAGE")"
          authority_architecture="$(docker image inspect --format '{{.Architecture}}' "$ECHO_CLEAN_AUTHORITY_IMAGE")"
          authority_source_sha="$(docker image inspect --format '{{index .Config.Labels "org.opencontainers.image.revision"}}' "$ECHO_CLEAN_AUTHORITY_IMAGE")"
          test "$authority_platform" = linux
          test "$authority_architecture" = arm64
          test "$authority_source_sha" = "$GITHUB_SHA"
          authority_node_version="$(docker run --rm --entrypoint node "$ECHO_CLEAN_AUTHORITY_IMAGE" --version)"
          test "$authority_node_version" = "v$PRODUCT_NODE_VERSION"

          runtime_profile="$RUNNER_TEMP/echo-clean-v1-runtime-profile.json"
          npm run profile:clean-v1-runtime -- "$deployment" "$runtime_profile"
          node tools/clean-v1-runtime-profile.mjs validate "$runtime_profile" >/dev/null
          runtime_profile_sha="$(node tools/clean-v1-runtime-profile.mjs digest "$runtime_profile")"
          export ECHO_CLEAN_RELEASE_ID="clean-v1-ci-${GITHUB_SHA:0:12}"
          export ECHO_CLEAN_RUNTIME_PROFILE_SHA256="$runtime_profile_sha"
          authority_local_args=(
            --state-dir "$authority_local_state"
            --image "$ECHO_CLEAN_AUTHORITY_IMAGE"
            --source-revision "$GITHUB_SHA"
            --release-id "$ECHO_CLEAN_RELEASE_ID"
            --runtime-profile-sha256 "$ECHO_CLEAN_RUNTIME_PROFILE_SHA256"
            --no-build
          )
          release_draft="$RUNNER_TEMP/echo-clean-v1-authority-release-draft.json"
          release_record="$RUNNER_TEMP/echo-clean-v1-authority-release.json"
          node -e '
            const fs = require("node:fs");
            fs.writeFileSync(process.argv[1], JSON.stringify({
              schema_version: 1,
              kind: "echo-clean-v1-release",
              release_id: `clean-v1-ci-${process.env.GITHUB_SHA.slice(0, 12)}`,
              released_at: "2026-08-24T00:00:00Z",
              baseline_compatibility_class: "clean-v1",
              source_sha: process.env.GITHUB_SHA,
              authority_image: {
                reference: `123456789012.dkr.ecr.us-west-2.amazonaws.com/echo-brain/authority@sha256:${"b".repeat(64)}`,
              },
              person_client: {
                package: "@echo-brain/person-client",
                version: process.argv[2],
                artifact_url: "https://downloads.example.test/echo-brain-person-client.tgz",
                artifact_sha256: "c".repeat(64),
              },
              runtime_profile: {
                artifact_url: "https://downloads.example.test/echo-brain-authority-runtime-profile.json",
                artifact_sha256: process.argv[3],
                profile_version: "clean-v1-profile-1",
              },
            }));
          ' "$release_draft" "$(node -p 'require("./src/product/person-client/package.json").version')" "$runtime_profile_sha"
          node tools/clean-v1-release.mjs create "$release_draft" "$release_record"
          node tools/clean-v1-release.mjs validate "$release_record" >/dev/null
          release_record_sha="$(shasum -a 256 "$release_record" | awk '{print $1}')"

          receipt="$RUNNER_TEMP/echo-clean-v1-authority-ci-receipt.json"
          node - "$receipt" "$authority_platform" "$authority_architecture" \
            "$authority_source_sha" "$authority_node_version" "$runtime_profile_sha" \
            "$release_record_sha" <<'NODE'
          const fs = require("node:fs");
          const [output, platform, architecture, sourceSha, nodeVersion, profileSha, releaseSha] = process.argv.slice(2);
          const receipt = {
            authority: {
              architecture,
              node_runtime_version: nodeVersion,
              oci_source_sha: sourceSha,
              platform,
            },
            kind: "echo-clean-v1-ci-authority-receipt",
            release_record_sha256: releaseSha,
            runtime_profile_sha256: profileSha,
            schema_version: 1,
          };
          const canonical = (value) => {
            if (value === null || typeof value !== "object") return JSON.stringify(value);
            if (Array.isArray(value)) return `[${value.map(canonical).join(",")}]`;
            return `{${Object.keys(value).sort().map((key) => `${JSON.stringify(key)}:${canonical(value[key])}`).join(",")}}`;
          };
          fs.writeFileSync(output, `${canonical(receipt)}\n`, { encoding: "utf8", mode: 0o600 });
          NODE
          node -e '
            const fs = require("node:fs");
            const receipt = JSON.parse(fs.readFileSync(process.argv[1], "utf8"));
            if (
              receipt.kind !== "echo-clean-v1-ci-authority-receipt" ||
              receipt.authority?.platform !== "linux" ||
              receipt.authority?.architecture !== "arm64" ||
              receipt.authority?.oci_source_sha !== process.env.GITHUB_SHA ||
              !/^v22\.22\.1$/.test(receipt.authority?.node_runtime_version ?? "") ||
              !/^[0-9a-f]{64}$/.test(receipt.runtime_profile_sha256 ?? "") ||
              !/^[0-9a-f]{64}$/.test(receipt.release_record_sha256 ?? "")
            ) throw new Error(`unexpected sanitized CI receipt: ${JSON.stringify(receipt)}`);
          ' "$receipt"
          cat "$receipt" >> "$GITHUB_STEP_SUMMARY"

          docker compose --file "$compose" config --quiet
          docker compose --file "$compose" --file "$deployment/compose.clean-v1.ec2.yaml" config --quiet
          docker run --rm \
            --env ECHO_CLEAN_AUTHORITY_HOST=localhost \
            --volume "$deployment/Caddyfile.clean-v1:/etc/caddy/Caddyfile:ro" \
            caddy:2.10.2-alpine@sha256:4c6e91c6ed0e2fa03efd5b44747b625fec79bc9cd06ac5235a779726618e530d \
            caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile
          docker run --rm \
            --env ECHO_CLEAN_AUTHORITY_HOST=localhost \
            --volume "$deployment/Caddyfile.clean-v1.ec2:/etc/caddy/Caddyfile:ro" \
            caddy:2.10.2-alpine@sha256:4c6e91c6ed0e2fa03efd5b44747b625fec79bc9cd06ac5235a779726618e530d \
            caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile

          docker run --rm --entrypoint sh "$ECHO_CLEAN_AUTHORITY_IMAGE" -ec '
              test -f services/organization-authority/dist/clean-live-main.js
              test -f services/organization-authority/dist/clean-reset-main.js
              test -f services/organization-authority/dist/clean-founder-main.js
              test ! -e services/organization-authority/dist/main.js
              test ! -e services/organization-authority/dist/composition/cli.js
              test -z "$(find packages services -type f \( -name "*.d.ts" -o -name "*.tsbuildinfo" \) -print -quit)"
            '

          # The local harness owns synthetic reset, seed, Compose lifecycle,
          # in-container descriptor, Caddy TLS, and Person-client descriptor
          # proof. The exact CI image and release-tuple proofs remain above.
          npm run authority:local -- up "${authority_local_args[@]}"

  required-checks:
    name: CI required checks
    if: ${{ always() }}
    needs: [check, person-client-package, authority-container]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every CI proof to succeed
        shell: bash
        env:
          CHECK_RESULT: ${{ needs.check.result }}
          PERSON_CLIENT_PACKAGE_RESULT: ${{ needs.person-client-package.result }}
          AUTHORITY_CONTAINER_RESULT: ${{ needs.authority-container.result }}
        run: |
          set -euo pipefail
          test "$CHECK_RESULT" = success
          test "$PERSON_CLIENT_PACKAGE_RESULT" = success
          test "$AUTHORITY_CONTAINER_RESULT" = success
