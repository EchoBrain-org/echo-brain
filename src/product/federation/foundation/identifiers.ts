/** Product compatibility surface for federation identifiers and timestamps. */
export {
  assertFederationId,
  assertUtcMillisecondTimestamp,
  federationId,
} from '@echo-brain/federation-protocol';
export type { FederationIdPrefix } from '@echo-brain/federation-protocol';
